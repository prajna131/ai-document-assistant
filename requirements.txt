# Pinned to the LangChain 0.3 series: these support Python 3.13 (via numpy 2.x
# wheels, so nothing compiles) AND keep the stable API this project is written
# for. Avoid LangChain 1.x, which reorganizes several imports.

# Core RAG framework
langchain>=0.3.20,<0.4
langchain-community>=0.3.20,<0.4
langchain-google-genai>=2.0,<3
langchain-text-splitters>=0.3,<0.4

# Vector database
chromadb>=0.5,<0.7
langchain-chroma>=0.1.4,<0.3

# LLM / Google Gemini
google-generativeai>=0.8

# Document loading
pypdf>=5.0

# Web UI
streamlit>=1.40

# Env variables
python-dotenv>=1.0

# Makes Python HTTPS use the OS (Windows) certificate store
# (fixes SSL errors on networks that inspect traffic)
truststore>=0.10
